[workspace]
resolver = "2"

members = [
  "libDF",
  "pyDF",
  "pyDF-data",
  "ladspa",
  "demo",
]

[patch.crates-io]
bitflags = { path = "vendor/bitflags-0.8.2" }

[profile.dev]
opt-level = 1
incremental = false
lto = false

[profile.release]
debug = false
opt-level = 3
lto = "thin"

[profile.release-lto]
inherits = "release"
debug = false
lto = "fat"
strip = true
panic = "abort"
codegen-units = 1
